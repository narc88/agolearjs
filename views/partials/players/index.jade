h3 Lista de Ligas
table.table.table-condensed.table-hover
	tr(ng-repeat="player in players") 
		td
			p {{player.name}} 
			p {{player.dni}}
			a(class="btn btn-primary", role="button", href="/players/{{player._id}}") Ver