div(class="panel-group", id="accordion", role="tablist", aria-multiselectable="true")
	div(class="panel panel-default")
		div(ng-repeat="participator in zone.participations",class="panel-heading", role="tab", id="headingOne")
			h4(class="panel-title")
				a(data-toggle="collapse", data-parent="#accordion", data-target="#collapse{{participator._id}}", aria-expanded="true", aria-controls="collapseOne")
					span {{participator.team_name}}
			div(id="{{participator._id}}", class="panel-collapse collapse", role="tabpanel", aria-labelledby="headingOne")
				div(class="panel-body")
					ul
						li(ng-repeat="player in participator.players")
							a(href="/players/{{player._id}}")  {{player.name}} {{player.last_name}}
							if(RolesHelper.admin(user))
								span(class="glyphicon glyphicon-remove", aria-hidden="true" , ng-click="removePlayer(player._id)")